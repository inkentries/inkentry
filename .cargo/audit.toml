[advisories]
# No advisory is ignored. `cargo audit` still prints paste's informational
# `unmaintained` warning (RUSTSEC-2024-0436, reached only through the dev-only
# tokenizers dependency); a warning does not fail the audit.
ignore = []
