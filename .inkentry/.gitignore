# Machine-specific SQLite, regenerated by `inkentry index`.
index.db*
memory.db*
# Per-run index lock + its pid sidecar (holds a local process id).
index.lock*
# Diagnostics from the detached background index phases.
*.log
